import React from "react";
import { useParams } from "react-router-dom";

const User = () => {
  //   const { userId } = useParams();
  //   if (!["name", "age", "email"].includes(userId)) {
  //     return <div> No Item</div>;
  //   }

  return <div>{/* <p>{userId}</p> */}</div>;
};

export default User;
